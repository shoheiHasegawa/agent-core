# 📈 実装・修正進捗管理 (Implementation Progress)

改善アイデアの選定、およびタスクの消化状況をトラッキングします。

---

## 🚦 進捗サマリー（2026/07/12 更新）
*   **現在のフェーズ**: 🎉 **本番移行準備・旧システム並行稼働対応フェーズ（完了）**
    *   本番稼働中のASISコードとの差分検証完了（デグレゼロ証明）。
    *   旧出欠表コピペ用の `IF_Legacy_Export` シート自動生成機能の実装完了。
    *   技術仕様書（`System_Spec.md`）の分離と、運用マニュアル（`StudyGroupAttendance_Manual.md`）の現場向け超実践的アップデート完了。
    *   残タスクはヘッダー配色の最終調整（Idea-19）とグリッド構造化（Idea-20）のみ。
*   **進行中 (In Progress)**: 1 件
*   **バックログ (Backlog)**: 1 件

---

## 📋 タスクボード

### ✅ 完了 (Done)
- [x] **[Feature] 旧出欠表コピペ用 `IF_Legacy_Export` シートの自動生成ロジック追加**
    *   既存のシステム（ASIS）へデグレなくアドオンとして実装。旧記号（○、△等）への自動変換機能。
- [x] **[Review] ASIS稼働中ソースコードとの完全性・差分検証**
    *   ユーザー提供のASISソースと照合し、これまでの修正が既存ロジックを一切破壊していないことを証明完了。
- [x] **[Doc] 運用マニュアルの大幅アップデートと仕様書の分離**
    *   現場向けのコピペ用チャットテンプレート、不正対策解説を追加した運用マニュアルの正本化。NotebookLM向け技術仕様書の作成完了。
- [x] **[Code] GAS・自動生成スクリプト（`system_builder.js`）の実装**
    *   フォーム作成・シート間連携・複雑な出欠判定マトリクス（K2のMAKEARRAY）の自動展開を実装完了。
- [x] **[Code] テンプレート内蔵GAS（`template_bound_script.js`）の実装**
    *   フォームB（事前連絡）のプルダウンを自動同期する深夜バッチおよび手動同期機能を実装。
- [x] **[Idea-4 / Idea-5] ログシートの「氏名参照」列の導入・MAXによる最新レコード取得**
    *   Log側の視認性向上機能（VLOOKUP）と、重複打刻時に最新の打刻のみを評価する処理を既に実装済み。
- [x] **[Idea-8] カスタムメニューからの「数式リセット（修復）」機能の実装**
    *   管理者がマスタ行削除で数式を壊した場合の自己修復機能（`resetFormulas`）を実装済み。
- [x] **[Doc] 次期実装（Idea-15等）に向けた設計情報・仕様群の正本化**
    *   **Phase 1 完了**: ハッシュ認証などの新アーキテクチャに関する仕様および実装計画のドキュメント（`Issue_Log`, `Ideas`, `System_Specification`）更新が完了。
- [x] **[Idea-15] フォームの動的フェーズ管理とハッシュ認証の導入**
    *   *Issue-2, Issue-8対策*: シークレットキーを用いたハッシュ認証、および当日打刻フォームの「タイトル動的変更」「受付制御」を実装済み。
- [x] **[Idea-10] 過去日入力防止・フォーム同期機能の実装**
    *   *Issue-5対応*: 事前連絡フォームの日付プルダウン化と、Schedule_DBとの連動（過去日除外）を実装済み。
- [x] **[Idea-3] 動的Prefilled URLによる不正防止（Idea-15へ統合・進化）**


---

### ⏳ 進行中 (In Progress)
- [ ] **[UX/UI] グリッド・配色最終調整（残タスク）**
    - [ ] [Idea-19] ヘッダー配色のアクセシビリティ改善 (Issue-12)

---

### 📝 未着手・次期実装計画 (Backlog)
- [ ] **[Idea-20] Progress_Master 集計グリッドの色付け・構造化**
    *   *Issue-13対応*: K列以降の日付エリアに対し、列幅の最適化や背景色の縞模様化を施し、多人数・多回数でも目が滑らないUIを実現する。

---

### ✅ 完了 (Done)
- [x] **[Idea-9] 集計ヘッダーを「予定表マスタベース」に固定**
    *   *Issue-4対応*: `Progress_Master` のK1セルの動的展開を `Schedule_DB` 基準に固定完了。
- [x] **[Idea-1] 各ログシートへの「論理削除フラグ」の導入** ➔ **不採用**
- [x] **[Idea-2] 数式内の `INDIRECT` 活用** ➔ **不採用**
- [x] **[Idea-22] 各ログシートへの「システム・アラート（MSG）」列の実装**
    *   *Issue-16, 17対応*: Log_AttendanceのI列・Log_NoticeのH列に「⚠️ システムMSG」列を実装。エラー理由をテキストで表示するロジックを実装済み。
- [x] **[Idea-18] Schedule_DB の種別プルダウン化**
    *   *Issue-15対応*: 「通常」「補講」のみ許容するデータ入力規則をC列に実装済み。
- [x] **[Idea-21] 判定エリアの無色セルの排除（薄いグレー化）**
    *   *Issue-14対応*: Progress_Master K2:ZZ範囲の未来日セルに薄いグレー(`#f3f3f3`)の条件付き書式を実装済み。
- [x] **[Idea-16] Progress_Master/Dashboardへのアラートサマリー表示**
    *   *Issue-9対応*: Dashboard A5セルにCOUNTIFによるエラー件数集計と警告メッセージを実装済み。
- [x] **[Idea-17] マスタ書式の強制置換・修復機能**
    *   *Issue-11対応*: onEditRouterでMember_DB A列の書式をリアルタイム強制、resetFormulasで全社員番号列の書式一括修復を実装済み。

---

### ❌ 不採用・アーカイブ (Archived)
- [x] **[Idea-3] 動的Prefilled URLによる不正防止（Idea-15へ統合・進化）**
    *   単なるパラメータ付与ではなく「隠し項目とTimestamp突合」へアーキテクチャが洗練されたため、Idea-15として計画を再定義して本項目はクローズ。
